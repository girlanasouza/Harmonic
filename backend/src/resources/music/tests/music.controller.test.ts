describe("music.controller.ts", () => {
  describe("streamingMusic", () => {
    it("should stream music correctly", async () => {
      expect(true).toBe(true);
    });

    it("should handle file not found error", async () => {
      expect(true).toBe(true);
    });
  });
});
